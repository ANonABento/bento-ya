# Handoff Prompt — 2026-04-06

You're working on bento-ya at /Users/bentomac/bento-ya — a Tauri desktop app (React 19 + Rust) for orchestrating AI coding agents via a kanban board with automated pipeline triggers.

Read these files first:
- CLAUDE.md (project instructions, architecture, conventions)
- .tickets/_docs/NEXT_STEPS.md (current state)
- .tickets/_docs/UI_RESTRUCTURE.md (UI redesign spec)

## Last Session (2026-04-06)

Shipped v2.0 Tier 1 features, MCP HTTP API bridge, crash fixes, agent session registration, PipeTransport streaming. 20 commits on main. E2E pipeline confirmed: MCP move_task → HTTP API → pipeline → PipeTransport spawns `claude --output-format stream-json` → agent runs → creates files + branch.

## Priority Tasks

### 1. Frontend agent chat wiring (BLOCKING — agents run but chat panel is empty)

`useChatSession` hook only shows interactive sessions, not trigger-spawned ones. Bridge already emits `agent:stream`, `agent:tool_call`, `agent:complete` events (confirmed via debug logs). Need: agent panel subscribes to these events when a task has `agentSessionId` set.

Files: `src/hooks/chat-session/use-chat-session.ts`, `src/components/panel/agent-panel.tsx`

### 2. Auto-create worktree for triggered agents

When `spawn_cli_trigger_task` fires, auto-create worktree before spawning. Pass `worktree_path` as `working_dir` to PipeTransport. This prevents the agent from switching branches on the main repo (which crashed `cargo tauri dev` last session).

Files: `src-tauri/src/chat/bridge.rs`, `src-tauri/src/git/branch_manager.rs`

### 3. UI restructure (spec at `.tickets/_docs/UI_RESTRUCTURE.md`)

- Click card → expands inline with full detail (description, branch, checklist, etc.)
- Agent chat → right slide-in panel (chat only, no detail panel)
- Remove split view detail panel

### 4. Full e2e test: task → agent → auto-advance → PR on GitHub

- Bento-ya Dev workspace pointed at /Users/bentomac/bento-ya
- Working column: spawn_cli trigger (claude /start-task sonnet, auto-advance on agent_complete)
- Done column: create_pr trigger (base: main)
- Test task "Update README with v2.0 features" exists with branch `bentoya/update-readme-with-v2-0-features`

## Key Architecture Notes

- MCP server (`bento-mcp`) now proxies mutations through Tauri app's HTTP API (port in `~/.bentoya/api.port`)
- Trigger-spawned agents use `PipeTransport` (not PTY) for structured JSON event streaming
- Agent sessions registered in DB so UI can track them
- Bridge emits both `pty:{taskId}:output` (terminal) and `agent:stream` (chat panel) events

Communicate results via Discord (chat_id: 1485889678996406395).
