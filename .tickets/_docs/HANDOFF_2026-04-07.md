# Handoff Prompt — 2026-04-07

You're working on bento-ya at /Users/bentomac/bento-ya — a Tauri desktop app (React 19 + Rust) for orchestrating AI coding agents via a kanban board with automated pipeline triggers.

Read these files first:
- CLAUDE.md (project instructions, architecture, conventions)
- .tickets/_docs/UNIFIED_CHAT.md (unified chat system — phases 1-5 done, phase 6 remaining)
- .tickets/_docs/UI_RESTRUCTURE.md (done — inline expanded cards + chat panel)

## Last Session (2026-04-07)

Shipped UI restructure, agent chat wiring, auto-worktree, resizable panels, and dependency line fixes. 8 commits on `feat/readme-v2-features`.

Key changes:
- Click card → expands inline (description, branch, changes, commits) + opens resizable agent chat panel
- Agent chat panel catches up on trigger-spawned agent output via `agent-streaming-store` (race condition fix)
- Trigger-spawned agents auto-create git worktree before spawning (sandboxed)
- Shared `useResizablePanel` hook + `ResizeHandle` component (used by both panels)
- Dependency SVG lines use scroll-content coordinates (fixed horizontal scroll issues)
- Scroll centering: rAF loop locks column at center during panel animation

## Priority Tasks

### 1. Terminal/bubble view toggle (PRIMARY)

Add xterm.js terminal view as alternative to bubble chat in the agent panel. Backend transport switching already architected.

**Step 1: Terminal view component**
- New `src/components/panel/terminal-view.tsx`
- Wraps xterm.js with WebGL addon
- Listens for `pty:{taskId}:output` events (base64 raw bytes)
- Handles `write_to_pty` for user input, `resize_pty` on container resize
- Reuse existing `src/lib/xterm-theme.ts`

**Step 2: View toggle in AgentPanel**
- Toggle button in agent panel header: bubble ↔ terminal
- State: `chatView: 'bubble' | 'terminal'` in ui-store (persisted)
- Bubble mode: current `ChatHistory` + `ChatInput` (PipeTransport)
- Terminal mode: `TerminalView` component (PtyTransport)

**Step 3: Transport switching (backend)**
- New Tauri command: `switch_agent_transport(taskId, transportType)`
- Suspends current session (saves resume ID), spawns with other transport using `--resume`
- `SessionRegistry` + `UnifiedChatSession` already support this
- `bridge.rs` already handles both transport types

**Step 4: Phase 6 cleanup**
- Delete `src-tauri/src/process/pty_manager.rs` (325 LOC)
- Delete `src-tauri/src/process/agent_runner.rs` (193 LOC)
- Rewire `commands/terminal.rs` to use `SessionRegistry` + `PtyTransport`
- Rewire `commands/siege.rs` to use unified sessions
- Delete `process/` module (~525 lines removed)

Files: `src-tauri/src/chat/`, `src-tauri/src/process/`, `src-tauri/src/commands/terminal.rs`, `src/components/panel/agent-panel.tsx`

### 2. Dockable panel layout system (BACKLOG)

VS Code-style dockable panels. Multiple panels (chef, agent chat, terminal, file viewer, changes) dock to left/right/bottom zones. Tab stacking when same zone. Drag-to-dock. Each zone resizable. Layout persisted. Ticket exists in Backlog.

### 3. Full e2e test (PENDING)

task → agent → auto-advance → PR on GitHub. Board already configured:
- Working column: spawn_cli trigger (claude /start-task sonnet, auto-advance on agent_complete)
- Done column: create_pr trigger (base: main)

### 4. UI polish items noticed during testing
- Scroll centering uses rAF lock — works but could be smoother
- Expanded card description shows when card description is hidden in settings (minor)

## Key Architecture Notes

- Backend unified chat: `src-tauri/src/chat/` (transport.rs, session.rs, registry.rs, bridge.rs, chef.rs)
- Legacy process layer: `src-tauri/src/process/` (pty_manager.rs, agent_runner.rs) — to be deleted in Phase 6
- Frontend chat: `src/hooks/chat-session/use-chat-session.ts` (unified hook for both panels)
- Agent streaming catchup: `src/stores/agent-streaming-store.ts` (global event capture for late-opening panels)
- Shared resize: `src/hooks/use-resizable-panel.ts` + `src/components/shared/resize-handle.tsx`
- UI state: `src/stores/ui-store.ts` (expandedTaskId, viewMode, panel widths, docks)

Communicate results via Discord (chat_id: 1485889678996406395).
